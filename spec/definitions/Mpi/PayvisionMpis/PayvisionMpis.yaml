description: Payvision Mpis
discriminator: name
required:
  - name
properties:
  name:
    allOf:
      - $ref: "#/definitions/MpiName"
    enum:
     - PayvisionMpi
     - CardinalCommerce
     - Other
