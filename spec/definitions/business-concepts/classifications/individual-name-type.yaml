description: The Individual Name Type resource.
type: object
properties:
  id:
    description: The resource's unique identifier.
    readOnly: true
    allOf:
      - $ref: "#/definitions/resourceId"
  name:
    description: The scheme value.
    type: string
    example: "Alias"
  description:
    description: The scheme value description.
    type: string
    example: "An alternate name used by a person."