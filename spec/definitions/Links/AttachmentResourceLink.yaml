type: object
allOf:
  - $ref: "#/definitions/NewLink"
properties:
  rel:
    description: The link type
    type: string
    enum:
      - customer
      - dispute
      - invoice
      - note
      - payment
      - plan
      - product
      - subscription
      - transaction
required:
  - rel
